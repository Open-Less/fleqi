#!/bin/sh
# 本地提交信息预检：与 CI 的 PR 规范门禁共用 scripts/check-commits.mjs。
# 启用：pnpm hooks:install（把 core.hooksPath 指向本目录）。
node "$(git rev-parse --show-toplevel)/scripts/check-commits.mjs" commit --file "$1" || {
  echo '提交被拦下：请修正提交信息后重试（git commit --no-verify 可跳过，CI 仍会拦）。'
  exit 1
}
