{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "main-window",
  "description": "Local Fleqi dashboard and attached bar; privileged work uses typed Rust commands",
  "windows": ["main", "settings", "agent-bar"],
  "permissions": ["core:default", "core:window:allow-start-dragging"]
}
